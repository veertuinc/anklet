# Note: Don't specify any credentials for the plugin. We will inject them into the config for you.
plugins:
  - name: GITHUB_HANDLER1_8_L_ARM_MACOS
    plugin: github
  - name: GITHUB_HANDLER2_8_L_ARM_MACOS
    plugin: github